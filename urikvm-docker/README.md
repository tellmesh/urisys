# Moved

Canonical location: [`urikvm-docker`](../../urikvm-docker/) at tellmesh workspace root.

Docker builds use tellmesh as context:

```bash
docker build -f urikvm-docker/Dockerfile /path/to/tellmesh
```
