# Moved

Canonical location: [`uristepper-docker`](../../uristepper-docker/) at tellmesh workspace root.

Docker builds use tellmesh as context:

```bash
docker build -f uristepper-docker/Dockerfile /path/to/tellmesh
```
