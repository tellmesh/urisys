# Moved

Canonical location: [`urirdp-docker`](../../urirdp-docker/) at tellmesh workspace root.

Docker builds use tellmesh as context:

```bash
docker build -f urirdp-docker/Dockerfile /path/to/tellmesh
```
