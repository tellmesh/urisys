"""env:// capability pack for urisys.

This pack exposes selected environment variables and Docker secrets through a
controlled URI interface. It is intended for boot-time configuration, diagnostics
and safe passing of non-sensitive runtime settings into handlers.
"""

__all__ = ["__version__"]
__version__ = "0.1.8"
