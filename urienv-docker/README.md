# Moved

Canonical location: [`urienv-docker`](../../urienv-docker/) at tellmesh workspace root.

Docker builds use tellmesh as context:

```bash
docker build -f urienv-docker/Dockerfile /path/to/tellmesh
```
