# Moved

Canonical location: [`urimail-docker`](../../urimail-docker/) at tellmesh workspace root.

Docker builds use tellmesh as context:

```bash
docker build -f urimail-docker/Dockerfile /path/to/tellmesh
```
