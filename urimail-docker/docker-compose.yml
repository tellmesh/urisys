# Local Mailpit mock for urimail:// (IMAP/SMTP dev).
# UI/API :8025, SMTP :1025
services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: urimail-mailpit
    ports:
      - "8025:8025"
      - "1025:1025"
    restart: unless-stopped
