[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "urimail"
version = "0.1.0"
description = "URI capability pack for mail:// operations."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["uricore>=0.1.0"]
keywords = ["uri", "urisys", "mail", "control-plane"]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["urimail*"]

[tool.setuptools.package-data]
"urimail" = ["manifest.yaml"]
