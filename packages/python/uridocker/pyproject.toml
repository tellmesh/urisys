[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uridocker"
version = "0.1.0"
description = "URI capability pack for docker:// operations."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["uricore>=0.1.0"]
keywords = ["uri", "urisys", "docker", "control-plane"]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["uridocker*"]

[tool.setuptools.package-data]
"uridocker" = ["manifest.yaml"]
