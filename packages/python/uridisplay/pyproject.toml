[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uridisplay"
version = "0.1.0"
description = "URI capability pack for display:// operations."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = ["uricore>=0.1.0"]
keywords = ["uri", "urisys", "display", "control-plane"]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["uridisplay*"]

[tool.setuptools.package-data]
"uridisplay" = ["manifest.yaml"]
