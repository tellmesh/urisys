# uribrowser-js

These packages are browser/Node URI capability packs. They intentionally do not include a global server. Use them with `@uricore/js` or with the `urisys` gateway.

Umbrella package for browser page capabilities. Prefer `uridom-js` and `uripage-js` in real projects.
