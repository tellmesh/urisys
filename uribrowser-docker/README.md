# Moved

Canonical location: [`uribrowser-docker`](../../uribrowser-docker/) at tellmesh workspace root.

Docker builds use tellmesh as context:

```bash
docker build -f uribrowser-docker/Dockerfile /path/to/tellmesh
```
