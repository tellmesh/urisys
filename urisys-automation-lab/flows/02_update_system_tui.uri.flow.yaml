flow:
  id: 02-update-system-tui
  description: System update through terminal/TUI (apt).

defaults:
  approved: true
  dry_run: true

do:
  - shell://apt-get:
      args: ["update"]
  - shell://apt-get:
      args: ["upgrade", "-y"]
  - shell://uname:
      args: ["-a"]
