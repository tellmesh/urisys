# Moved

Canonical location: [`urisys-automation-lab`](../../urisys-automation-lab/) at tellmesh workspace root.

Docker builds use tellmesh as context:

```bash
docker build -f urisys-automation-lab/Dockerfile /path/to/tellmesh
```
