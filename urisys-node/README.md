# Moved

Canonical location: [`urisys-node`](../../urisys-node/) at tellmesh workspace root.

Docker builds use tellmesh as context:

```bash
docker build -f urisys-node/Dockerfile /path/to/tellmesh
```
