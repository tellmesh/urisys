# 01-call-uri

Pojedyncze wywolania URI przez CLI `urisys`.

```bash
bash examples/01-call-uri/run.sh
```

Skrypt otwiera mockowa strone przez `browser://`, a potem wykonuje suchy restart
kontenera przez `docker://`.
