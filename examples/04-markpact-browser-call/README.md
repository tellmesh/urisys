# 04-markpact-browser-call

Wywolanie `browser://` przez Markpact kompilowany w locie do runtime manifestu.

```bash
bash examples/04-markpact-browser-call/run.sh
```

Wymaga sibling repo `markpact-contracts` albo poprawnie ustawionego
`TELLMESH_ROOT`.
