# 05-markpact-showcase-run-flow

Smoke test embedded flows z `uribrowser.showcase.markpact.md`.

```bash
bash examples/05-markpact-showcase-run-flow/run.sh
```

Skrypt wykonuje analyze, `markpact run-flow` oraz cienkie `markpact run --as flow`.
Wymaga sibling repo `markpact-contracts` i packow uzytych przez showcase.
