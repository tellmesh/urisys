# 06-frontend

Minimalny frontend `uricontrol-js` z lokalnym DOM URI i HTTP clientem do
`urisys serve`.

```bash
bash examples/06-frontend/run.sh
```

Domyslnie serwuje `http://127.0.0.1:8088/index.html`. Przycisk HTTP wymaga
osobno uruchomionego backendu `urisys serve` na porcie `8789`.
